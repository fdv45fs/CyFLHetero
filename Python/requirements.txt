# ===================================
# Project Dependencies
# ===================================
# NOTE: For better control, use specific version files:
#   - requirements_gpu.txt (for CUDA 11.8)
#   - requirements_cpu.txt (for CPU-only)
#
# See SETUP.md for installation instructions
# ===================================

Flask==3.1.0
scikit_learn==1.5.2
torch==2.4.0
torch_geometric==2.6.1

# NOTE: torch-cluster is REQUIRED for Node2Vec but must be installed separately
# See requirements_gpu.txt or requirements_cpu.txt for complete installation guide
